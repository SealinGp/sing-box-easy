// Outbound Node Rules page strings.
export default {
  title: 'Outbound Node Rules',
  subtitle: 'Auto-organize subscription nodes into Filters (keyword-matched buckets) and Groups (sets of Filters).',
  preview: 'Preview',
  applyNow: 'Apply Now',
  applying: 'Applying…',
  applied: 'Node rules applied to config.',
  templates: 'Quick-add templates',
  filters: 'Filters (Region Nodes)',
  groups: 'Groups (Group Nodes)',
  addFilter: 'Add Filter',
  addGroup: 'Add Group',
  modal: {
    addFilter: 'Add Filter',
    editFilter: 'Edit Filter',
    addGroup: 'Add Group',
    editGroup: 'Edit Group',
  },
  filterName: 'Filter name (e.g. Asia, YouTube)',
  groupName: 'Group name (e.g. Streaming)',
  priority: 'Priority (lower = matched first)',
  priorityLabel: 'Priority',
  priorityHint: 'lower number = matched first',
  urltestSettings: 'urltest health check',
  intervalHint:
    'Every node in this filter is tested on each tick, so short intervals on large filters generate sustained load. The server raises the interval automatically when the filter is big.',
  testUrl: 'Test URL',
  testInterval: 'Interval',
  testTolerance: 'Tolerance (ms)',
  matcherValue: 'value (e.g. Streaming, 香港, 🇭🇰)',
  addMatcher: 'Add matcher',
  addCountry: 'Search a country code…',
  noCodeMatches: 'No matching codes',
  // Node picker inside the matchers section: adds an exact-tag keyword
  // matcher. The only way a `direct` outbound joins a filter.
  includeNode: 'Search a node to include…',
  directBadge: 'direct',
  // Group members named directly (not via a filter) — e.g. `direct`.
  groupExtraTags: 'Outbounds in this group',
  groupExtraTagsHint: '— picked by tag, e.g. a direct bypass entry',
  groupAddTag: 'Search an outbound to add…',
  groupTagBadgeTitle: 'named directly by this group, not matched by a filter',
  groupTagsEmpty: 'No outbounds known yet. Run a preview first.',
  // Exclude (deny-list): nodes kept out even when a matcher hits them.
  excludes: 'Except nodes',
  excludesHint: '— matched but kept out (e.g. drop relay_bwh_us1 from US)',
  addExclude: 'Add exclude',
  excludeNode: 'Search a node to exclude…',
  excludeBadgeTitle: 'excluded — never added even if matched',
  noNodeMatches: 'No matching nodes',
  // How-matching-works explainer (shown inside the filter editor)
  help: {
    toggle: 'How does matching work?',
    intro:
      'A node joins this filter when its name matches ANY one of the matchers below (OR). Matching is case-insensitive and looks for your text anywhere inside the node name.',
    keywordTitle: 'keyword — free text',
    keywordDesc:
      'Substring match. The node joins if its name contains your text. e.g. "Netflix" matches "🇭🇰HK-Netflix-01"; "香港" matches "香港IEPL专线".',
    codeTitle: 'code — country / region (recommended)',
    codeDesc:
      'Pick a region and it expands to ALL common spellings at once — Chinese, English, the 2-letter code and the flag emoji. e.g. HK matches 香港 / Hong Kong / HK / 🇭🇰. No need to add each spelling by hand.',
    emojiTitle: 'emoji — flag character',
    emojiDesc:
      'Exact match of an emoji flag. e.g. 🇭🇰 matches any name containing that flag. Use when node names carry only the flag.',
    multi:
      'A node can belong to several filters at once. Nodes that match no filter fall through to the Fallback.',
    codesTitle: 'Supported region codes',
    codesHint: 'Hover a code to see every spelling it expands to.',
  },
  noMatchers: 'no matchers',
  noFilters: 'no filters selected',
  noGroups: 'No groups yet.',
  fallback: 'Fallback',
  fallbackHint: 'Catches every node that matches no other Filter. Cannot be deleted.',
  edit: 'Edit',
  delete: 'Delete',
  save: 'Save',
  cancel: 'Cancel',
  nameRequired: 'Name is required.',
  confirmDeleteFilter: 'Delete filter "{name}"?',
  confirmDeleteGroup: 'Delete group "{name}"?',
  previewResult: 'Preview',
  endpoints: '{n} endpoints',
  unmatched: '{n} node(s) matched no Filter → fall through to the fallback.',
}
